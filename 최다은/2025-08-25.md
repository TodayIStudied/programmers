<!--
- 제목은 연-월-일.md
- 아래 템플릿 기준으로 진행하되 필요 시. 변형 가능
- 파일은 programmers/이름/연-월-일.md 
-->
### 문제 설명  
- 풀이 URL : [짝지어 제거하기](https://github.com/thisischeese/codingTest/blob/main/%ED%94%84%EB%A1%9C%EA%B7%B8%EB%9E%98%EB%A8%B8%EC%8A%A4/2/12973.%E2%80%85%EC%A7%9D%EC%A7%80%EC%96%B4%E2%80%85%EC%A0%9C%EA%B1%B0%ED%95%98%EA%B8%B0/%EC%A7%9D%EC%A7%80%EC%96%B4%E2%80%85%EC%A0%9C%EA%B1%B0%ED%95%98%EA%B8%B0.py)

### 접근 방식 : 1. 완탐 + 스택 
- 왜 이 방식으로 풀었나?
  - 진짜 러프하게 완탐 생각해보면 2^n~=시간복잡도~=n! 인 풀이 나옴
  - 문자열 길이가 1e6이기 때문에 최대한 1번만 스캔해서 끝낼 수 있도록 구현해야 함.
  - 따라서 괄호 매칭 문제와 유사하게 스택으로 풀이하였다. 
  ```
  def solution(words):
    words = list(words)
    stack = []
    for word in words:
        if(len(stack) and stack[-1]==word):
            stack.pop()
        else:
            stack.append(word)
    return 0 if len(stack) else 1

  """
  1. 문자열을 문자 단위로 split시켜 순회할 수 있는 자료구조로 변형시키기 
  2. 문자가 저장된 자료구조를 처음부터 순회하며 하나의 문자열 빼기  
  3. 스택에 문자열을 넣기 
      3.1 만약에 스택이 비었다. -> 그냥 넣는다. 
      3.2 만약 스택에 문자가 존재한다. 
          3.2.1 만약 맨 위가 동일한 문자일 경우 맨 위를 빼고 자신도 넣지 않기
          3.2.2 만약 맨 위가 동일하지 않으면 그냥 넣는다. 
  4. 다 돌았는데 스택에 아직도 문자가 남아있다면 실패 
  """
  ```
- 시간복잡도, 공간복잡도 관점에서의 성능 비교
  - 메모리: 21.5 MB, 시간: 86.82 ms

### 접근 방식 : 2. DFS 
- 왜 이 방식으로 풀었나?
  - 불필요한 메모리 제거
  - python문자열 iterable하니까 굳이 list화시킬 필요 없음 
  ```
  def solution(words):
    stack = []
    for word in words:
        stack.pop() if(len(stack) and stack[-1]==word) else stack.append(word) 
    return 0 if stack else 1

  """
  1. 문자열을 문자 단위로 split시켜 순회할 수 있는 자료구조로 변형시키기 
  2. 문자가 저장된 자료구조를 처음부터 순회하며 하나의 문자열 빼기  
  3. 스택에 문자열을 넣기 
      3.1 만약에 스택이 비었다. -> 그냥 넣는다. 
      3.2 만약 스택에 문자가 존재한다. 
          3.2.1 만약 맨 위가 동일한 문자일 경우 맨 위를 빼고 자신도 넣지 않기
          3.2.2 만약 맨 위가 동일하지 않으면 그냥 넣는다. 
  4. 다 돌았는데 스택에 아직도 문자가 남아있다면 실패 
  """
  ```
- 시간복잡도, 공간복잡도 관점에서의 성능 비교
  - 메모리: 13.7 MB, 시간: 86.80 ms



### 회고 
- 몇 번만에 성공했는지 : 1
- 잘한 점 : 스택 문제 알아보기 힘든데 설계 과정에서 스택 문제임을 한 번에 알아본 것 
- 개선할 점 : 
