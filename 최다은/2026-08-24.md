<!--
- 제목은 연-월-일.md
- 아래 템플릿 기준으로 진행하되 필요 시. 변형 가능
- 파일은 programmers/이름/연-월-일.md 
-->
### 문제 설명  
- 풀이 URL : [JadenCase 문자열 만들기]([문제-저장된-깃허브-URL](https://github.com/thisischeese/codingTest/tree/main/%ED%94%84%EB%A1%9C%EA%B7%B8%EB%9E%98%EB%A8%B8%EC%8A%A4/2/12951.%E2%80%85JadenCase%E2%80%85%EB%AC%B8%EC%9E%90%EC%97%B4%E2%80%85%EB%A7%8C%EB%93%A4%EA%B8%B0))

### 접근 방식 : 1. 단순 구현 
- 왜 이 방식으로 풀었나?
  - 공백을 리스트화, JadenCase 문자열 리스트화해서 마지막에 붙여서 반환하는 코드
  - 처음에 공백만 존재하는 케이스 생각 안하고 풀어서 1개 틀렸었음.
- 시간복잡도, 공간복잡도 관점에서의 성능 비교
  - 메모리: 11.6 MB, 시간: 0.06 ms
  - 최적화 전혀 고려 안하고 문자열끼리 덧셈 남발해서 코드 별로라고 생각.
  - 한 번은 무조건 처음부터 끝까지 훑어야 하는 것은 맞음. 근데 지금 2번 훑고 심지어 마지막에 합치기 위해 한 번 더 훑음. 

### 접근 방식 : 2. 단순 구현 
- 왜 이 방식으로 풀었나?
  - 단 한 번만 훑어도 JadenCase 문자열 만들 수 있도록 조건문으로 구현했음.
  - 훨씬 코드 명확해서 더 좋은 방법이라고 생각함. 이전 코드는 너무 예외 케이스가 자잘했음.
    ```
    def solution(words):
      words = list(words)
      for i in range(len(words)):
          if((i==0 or (i>0 and ord(words[i-1])==32)) and 97<=ord(words[i])<=122):
              words[i] = chr(ord(words[i])-32)
          elif((i>0 and ord(words[i-1])!=32) and 65<=ord(words[i])<=90):
              words[i] = chr(ord(words[i])+32)
      return "".join(words)
    '''
    1. 처음부터 문자열 스캔하기
    2. 만약 문자가 공백이거나 숫자이다 -> 그대로 추가
    3. 만약 문자가 소문자인데 이전 문자가 공백이거나 인덱스가 0이다 -> 대문자로 바꾸기
    4. 만약 문자가 대문자인데 이전 문자가 공백이 아니다 -> 소문자로 바꾸기 
    '''
    ```
- 시간복잡도, 공간복잡도 관점에서의 성능 비교
  - 메모리: 11.3 MB, 시간: 0.04 ms

### 회고 
- 몇 번만에 성공했는지 : 2 
- 잘한 점 : 문제 꼼꼼히 읽어서 처음에 틀렸을 때 공백이 문제임을 빠르게 파악했다는 것? 공백 테스트케이스 추가 굿 
- 개선할 점 : 생각 정리 덜 된 채로 우선 구현하다가 중간에 문제 발견했음. 이전까지 구현한 코드 살리려다보니 시간복잡도 측면에서 별로 좋지 못한 코드를 처음에 구현했음.
-           중간에 문제 발견해도 한글로 생각 정리 문서화한 후에 구현할 것.. 
