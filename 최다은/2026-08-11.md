<!--
- 제목은 연-월-일.md
- 아래 템플릿 기준으로 진행하되 필요 시. 변형 가능
- 파일은 programmers/이름/연-월-일.md 
-->
### 문제 설명  
- 풀이 URL : [[PCCE 기출문제] 9번 / 이웃한 칸](https://github.com/thisischeese/codingTest/tree/main/%ED%94%84%EB%A1%9C%EA%B7%B8%EB%9E%98%EB%A8%B8%EC%8A%A4/1/250125.%E2%80%85%EF%BC%BBPCCE%E2%80%85%EA%B8%B0%EC%B6%9C%EB%AC%B8%EC%A0%9C%EF%BC%BD%E2%80%859%EB%B2%88%E2%80%85%EF%BC%8F%E2%80%85%EC%9D%B4%EC%9B%83%ED%95%9C%E2%80%85%EC%B9%B8)

### 접근 방식 : 1. 구현 
- 왜 이 방식으로 풀었나?
  - 플러드필스러운 그냥.. 구현 문제라서..?
  - 처음에는 len(list(filter(lambda .. )))로 감싸서 구현했음. 그러나 이렇게까지 리스트를 만들 필요가 없다.. 
- 시간복잡도, 공간복잡도 관점에서의 성능 비교
  - 메모리: 11.3 MB, 시간: 0.03 ms
  - 사실 시간복잡도 측면에서 공간복잡도 더 희생해서 줄일 수 있을 것 같음. 비교하는 변수 등을 미리 공간 할당하면 되니까
  - 그러나 숫자가 너무 작아서 이 정도도 충분하다고 생각함..
    ```
    def solution(board, h, w):
        return len(list(filter(lambda p: 0<=p[0]<len(board) \
                               and 0<=p[1]<len(board[0])\
                               and board[p[0]][p[1]] == board[h][w],\
                               [(h+1,w),(h-1,w),(h,w+1),(h,w-1)])))

    ```

### 접근 방식 : 2. 구현인데 불린 조건문으로 1 OR 0을 SUM시킴 
- 시간복잡도, 공간복잡도 관점에서의 성능 비교
  - 메모리: 11.5 MB, 시간: 0.01 ms
    ```
    def solution(board, h, w):
    return sum(0<=p[0]<len(board) \
               and 0<=p[1]<len(board[0]) \
               and board[p[0]][p[1]] == board[h][w] \
               for p in [(h+1,w),(h-1,w),(h,w+1),(h,w-1)])

    ```

### 회고 
- 몇 번만에 성공했는지 : 2 
- 잘한 점 : 짧다.. 코드가.. 더 짧을 수 없을까? 
- 개선할 점 : 행, 열 길이 출력 문법을 틀려서 런타임 에러 발생했음.... 행 열 길이는 또 처음 틀려봄... 
